# Core data and pipeline dependencies (mt5linux is installed separately in
# .mt5linux-venv via mt5linux-lock.txt)
pandas
pandera
pyarrow
scikit-learn
numpy>=1.21.4,<2.0; platform_system != "Windows" and python_version < "3.12"
numpy>=2.1.3,<3.0; platform_system == "Windows" or python_version >= "3.12"
MetaTrader5; platform_system == "Windows"
joblib
pyyaml
psutil
GitPython
gdown
great_expectations<0.19
duckdb
dask[distributed]
pandas_datareader
lightgbm
torch  # required for autoencoder features
tqdm
scikit-optimize
scipy
river
kafka-python
redis
testcontainers[kafka,redis]
filelock
rpyc>=5.3
fastapi
uvicorn
uvloop; platform_system != "Windows"
mlflow
shap  # optional, used to compute SHAP feature importance
statsmodels
tsfresh
exchange-calendars
arch
requests
cryptography
yfinance
fredapi
httpx
grpcio
grpcio-tools
backtrader
freqtrade
PyWavelets
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-jaeger
opentelemetry-exporter-prometheus
opentelemetry-instrumentation-logging

# Heavy optional dependencies
# Ray and torch-geometric currently lack official Python 3.13 wheels; the
# distributed and graph trainers gracefully fall back when they are absent,
# so we remain on Python 3.10 by default until upstream support lands.
flaml>=2.3.6
ray==2.48.0; python_version < "3.13"
torch-geometric==2.6.1; python_version < "3.13"

# Reinforcement learning dependencies
stable-baselines3
sb3-contrib
gymnasium

# Natural language processing dependencies
transformers
diffusers

# Documentation toolchain
sphinx
myst-parser
sphinx-rtd-theme
mkdocs
