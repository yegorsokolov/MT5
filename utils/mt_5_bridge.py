#!/usr/bin/env python3
import time, sys
print("MT5 bridge placeholder started; replace me with your real bridge logic.", flush=True)
try:
    while True:
        time.sleep(60)
except KeyboardInterrupt:
    sys.exit(0)
