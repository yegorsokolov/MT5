training:
  batch_size: 128
  n_jobs: 8
  use_pseudo_labels: true
strategy:
  symbols:
    - EURUSD
  risk_per_trade: 0.05
alerting:
  telegram_bot_token: secret://alert-bot-token
  telegram_chat_id: secret://alert-chat
